const Site = (function Site() {
  // Function placeholder
  const doStuff = function doStuff() {
    // Do stuff
  }

  return {
    init() {
      // Initialise things
      doStuff();
    }
  }
});

Site.init();
